# Safe-Link Sandbox MVP 구현 계획서

## 1. 프로젝트 개요

### 서비스 소개

**Safe-Link Sandbox**는 의심스러운 URL을 안전하게 분석하는 웹 서비스입니다. 사용자가 클릭하기 전에 URL의 위험도를 미리 확인하고, 격리된 샌드박스 환경에서 웹페이지를 렌더링하여 스크린샷과 보안 분석 결과를 제공합니다.

### 목표

- **안전한 URL 미리보기**: 직접 방문하지 않고 웹페이지 내용 확인
- **위험도 분석**: 피싱, 악성코드, 의심스러운 패턴 탐지
- **시각적 피드백**: 스크린샷과 색상 코딩된 위험도 표시
- **빠른 응답**: 30초 이내 분석 완료

---

## 2. 기술 스택

| 구분 | 기술 | 버전 | 용도 |
|------|------|------|------|
| **Frontend** | React | 18.x | UI 프레임워크 |
| | CSS | - | 스타일링 |
| | Axios | 1.x | HTTP 클라이언트 |
| **Backend** | Node.js | 20.x | 런타임 |
| | Express | 4.x | 웹 프레임워크 |
| | Puppeteer | 22.x | 브라우저 자동화 |

---

## 3. 시스템 아키텍처

```
┌─────────────────────────────────────────────────────────────────┐
│                     Safe-Link Sandbox MVP                        │
├─────────────────────────────────────────────────────────────────┤
│                                                                  │
│  ┌──────────────────┐         ┌──────────────────────────────┐  │
│  │   React Client   │  HTTP   │      Backend Server          │  │
│  │  localhost:3000  │ ◄─────► │  (Express + Puppeteer)       │  │
│  │                  │   API   │      localhost:4000          │  │
│  └──────────────────┘         └──────────────────────────────┘  │
│                                          │                       │
│                                          ▼                       │
│                               ┌──────────────────────┐          │
│                               │  Puppeteer Sandbox   │          │
│                               │  (Headless Chrome)   │          │
│                               └──────────────────────┘          │
│                                                                  │
└─────────────────────────────────────────────────────────────────┘
```

### 데이터 흐름

```
사용자 URL 입력 → Frontend → Backend API → Puppeteer 샌드박스
                                              │
                    결과 표시 ← Frontend ← 분석 결과 + 스크린샷
```

---

## 4. 디렉토리 구조

```
safe-link-sandbox/
├── package.json              # 루트 패키지
├── MVP_PLAN.md               # 이 문서
│
├── backend/
│   ├── package.json
│   ├── server.js             # Express 서버
│   └── analyzer.js           # Puppeteer + 위험도 분석
│
└── frontend/
    ├── package.json
    ├── public/
    │   └── index.html
    └── src/
        ├── App.jsx           # 메인 컴포넌트
        ├── App.css           # 스타일
        └── index.js          # 진입점
```

---

## 5. API 명세

### POST /api/analyze

URL 전체 분석 (샌드박스 실행)

**Request:**
```json
{
  "url": "https://example.com"
}
```

**Response:**
```json
{
  "url": "https://example.com",
  "domain": "example.com",
  "isHttps": true,
  "pageTitle": "Example Domain",
  "screenshot": "data:image/png;base64,...",
  "riskLevel": "safe",
  "riskScore": 15,
  "riskFactors": [],
  "analysisTime": 3500
}
```

### 위험도 레벨

| 레벨 | 점수 | 색상 | 설명 |
|------|------|------|------|
| `safe` | 0-30 | 🟢 녹색 | 안전 |
| `warning` | 31-60 | 🟡 노랑 | 주의 |
| `danger` | 61-100 | 🔴 빨강 | 위험 |

---

## 6. 위험도 분석 기준

| 요소 | 점수 | 설명 |
|------|------|------|
| HTTPS 미사용 | +30 | HTTP 프로토콜 |
| 의심 키워드 | +40 | 긴급, 계좌, 확인 등 |
| 긴 도메인 | +20 | 30자 이상 도메인 |
| 단축 URL | +10 | bit.ly, tinyurl 등 |
| 리다이렉트 | +10/회 | 리다이렉트 횟수 |

---

## 7. 구현 단계

### Step 1: Backend 구현 (2-3시간)

```bash
cd backend
npm init -y
npm install express cors puppeteer
```

**server.js** - Express 서버 (CORS, 라우팅)
**analyzer.js** - Puppeteer 샌드박스 + 위험도 분석

### Step 2: Frontend 구현 (2-3시간)

```bash
npx create-react-app frontend
cd frontend
npm install axios
```

**App.jsx** - URL 입력, 로딩 상태, 결과 표시
**App.css** - 모바일 친화적 스타일

### Step 3: 통합 테스트 (1시간)

```bash
# 터미널 1: Backend
cd backend && node server.js

# 터미널 2: Frontend
cd frontend && npm start
```

---

## 8. 실행 방법

### 개발 서버 실행

```bash
# Backend (터미널 1)
cd backend
node server.js
# → http://localhost:4000

# Frontend (터미널 2)
cd frontend
npm start
# → http://localhost:3000
```

### API 테스트

```bash
curl -X POST http://localhost:4000/api/analyze \
  -H "Content-Type: application/json" \
  -d '{"url": "https://google.com"}'
```

---

## 9. 데모 시나리오 (영상 촬영용)

### 시나리오 1: 안전한 URL (30초)

1. `http://localhost:3000` 접속
2. `https://google.com` 입력
3. "분석하기" 클릭
4. 로딩 → 🟢 안전 결과 확인

### 시나리오 2: 의심스러운 URL (30초)

1. `http://example.com` 입력 (HTTP)
2. 🟡 주의 결과 확인
3. "HTTPS 미사용" 경고 표시

### 시나리오 3: 위험한 URL (30초)

1. 피싱 패턴 URL 입력
2. 🔴 위험 결과 확인
3. 상세 위험 요소 설명

---

## 10. 테스트 URL

### 안전 (Safe)
- https://google.com
- https://naver.com
- https://github.com

### 주의 (Warning)
- http://example.com (HTTPS 미사용)

### 위험 판정 테스트
- 긴급-계좌확인.com (의심 키워드)
- 30자 이상 긴 도메인

---

## 11. 보안 고려사항

- **Puppeteer 샌드박스 모드**: `--no-sandbox`, `--disable-setuid-sandbox`
- **타임아웃**: 30초 제한
- **로컬 IP 차단**: 127.0.0.1, 192.168.x.x 접근 금지
- **파일 다운로드 차단**: setDownloadBehavior 비활성화

---

## 12. 확장 계획 (Phase 2)

- [ ] Google Safe Browsing API 연동
- [ ] VirusTotal API 연동
- [ ] 분석 히스토리 저장
- [ ] KISA 신고 연동
- [ ] 모바일 앱 (React Native)

---

**작성일**: 2026-02-02
**버전**: MVP 1.0.0
