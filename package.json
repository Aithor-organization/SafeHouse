{
  "name": "safe-link-sandbox",
  "version": "1.1.0",
  "description": "URL 안전성 분석 샌드박스 API + AI 분석",
  "main": "server.js",
  "type": "module",
  "scripts": {
    "start": "node --env-file=.env server.js",
    "dev": "node --env-file=.env --watch server.js"
  },
  "dependencies": {
    "cors": "^2.8.5",
    "express": "^4.18.2",
    "express-rate-limit": "^7.1.5",
    "helmet": "^7.1.0",
    "puppeteer": "^24.36.1"
  },
  "engines": {
    "node": ">=20.0.0"
  }
}
